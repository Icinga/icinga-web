prefix=@prefix@
exec_prefix=@exec_prefix@
LOGDIR=@localstatedir@
CFGDIR=@sysconfdir@
BINDIR=@bindir@
CGIDIR=@sbindir@
HTMLDIR=@datarootdir@

INSTALL=@INSTALL@
INSTALL_OPTS=@INSTALL_OPTS@

install: 
	cd jit && make install
	cd ext3 && make install
	cd icingaScheduler && make install
	cd doctrine && make install
	cd agavi && make install
	cd phpseclib && make install
	cd jsgettext && make install 
