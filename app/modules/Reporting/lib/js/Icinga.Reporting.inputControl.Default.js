Ext.ns('Icinga.Reporting.inputControl');

Icinga.Reporting.inputControl.Default = Ext.extend(Ext.form.TextField, {
	constructor : function(config) {
		Icinga.Reporting.inputControl.Default.superclass.constructor.call(this, config);
	}
});